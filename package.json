{
  "name": "tex-two",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev --remote",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "db:migrate": "wrangler d1 migrations apply tex-two-db --remote",
    "db:migrate:local": "wrangler d1 migrations apply tex-two-db --local",
    "export-qr": "node scripts/export-qr.mjs"
  },
  "dependencies": {
    "hono": "^4.6.14",
    "qrcode": "^1.5.4"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241218.0",
    "@types/qrcode": "^1.5.5",
    "typescript": "^5.7.2",
    "wrangler": "^3.95.0"
  }
}
